@model IEnumerable<DoAn.Models.DMCHINHSACH>


@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="row" style="margin-left:10px">
    <div class="col-sm-12">
        <h2>DANH SÁCH DANH MỤC CHÍNH SÁCH</h2>
        <p><a class="btn btn-success" style="border-radius:0" href="/admin/dmchinhsaches/Create">THÊM MỚI</a></p>
        <table class="table table-bordered table-hover">
            <tr>
                <th>
                    Tên Danh Mục
                </th>
                <th>
                    Từ Khóa
                </th>
                <th>
                    Tiêu Đề Seo
                </th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.TenDM)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TuKhoa)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TieuDeSeo)
                    </td>
                    <td>
                        <a class="btn btn-warning btn-xs" style="border-radius:0" href="/Admin/dmchinhsaches/edit/@item.ID"><i class="fa fa-edit"></i></a> |

                        <a class="btn btn-danger btn-xs" style="border-radius:0" href="/Admin/dmchinhsaches/delete/@item.ID"><i class="fa fa-recycle"></i></a>
                    </td>
                </tr>
            }

        </table>
    </div>
</div>